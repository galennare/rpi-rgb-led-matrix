#include <Arduino.h>

#include "led-matrix.h"

using std::string;

namespace rgb_matrix {
    bool rgb_matrix::RGBMatrix::Options::Validate(string *err) const {
        
    }
}  // end namespace rgb_matrix